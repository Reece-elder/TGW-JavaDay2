package com.qa.demo;

public class UnitTestMethods {
	
	// Create a method that takes in two ints and returns the sum
	public static int addSum(int x, int y) {
		
		int result = x + y;
		return result;
		
	}
	
	// Exercise - Using new packages create the following: 
	// - Create a blackjack method, given 2 int values greater than 0, 
	// return whatever is closest to 21 without going over. If both go over 21 return 0
	// Test this method covering all edge cases (if both ints are over 0, if both are under 0 etc.)

}
